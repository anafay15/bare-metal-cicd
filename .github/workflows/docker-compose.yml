version: '3.8'
services:
  api:
    build: .
    container_name: project1_api
    ports:
      - "80:3000" # Map host port 80 to container port 3000
    restart: always # Production standard: ensure it boots if the VM restarts